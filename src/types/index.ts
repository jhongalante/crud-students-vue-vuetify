export interface IStudent {
  name: string;
  cpf: string;
  email: string;
  academicRecord: string;
}

export interface IUpdatedStudent {
  name: string;
  email: string;
  academicRecord: string;
}
